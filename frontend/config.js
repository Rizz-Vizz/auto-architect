// ⚠️  BEFORE DEPLOYING: replace the URL below with your deployed Zerops API
// subdomain. Find it in the Zerops GUI → api service → IP addresses & Public
// Routing. It looks like: https://api-xxxx-8000.prg1.zerops.app
//
// After deploying the frontend you can also change the URL at any time by
// clicking "API endpoint" at the bottom of the input panel — it persists in
// localStorage and survives re-deploys without a rebuild.
//
// For LOCAL development: leave this as http://localhost:8000.
// It already matches `npm start` in /backend and nothing else needs changing.
window.AUTO_ARCHITECT_CONFIG = {
  DEFAULT_API_BASE_URL: "https://api-237-8000.ny1.zerops.app",
};
