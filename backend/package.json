{
  "name": "auto-architect-backend",
  "version": "1.0.0",
  "description": "Turns a plain-English app description into a real zerops.yaml + architecture map, using Claude.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/integration.test.mjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "yaml": "^2.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
